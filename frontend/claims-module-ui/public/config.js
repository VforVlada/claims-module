// Runtime configuration, loaded before the Angular bundle. Deployments overwrite this file
// (see .github/workflows/ci.yml) so one build artifact can target any environment's API.
window.__CLAIMS_CONFIG__ = {
  apiOrigin: 'http://localhost:5299'
};
