server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Angular client-side routing: fall back to index.html for any unmatched path.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
